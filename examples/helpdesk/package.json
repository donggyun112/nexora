{
  "name": "@nexora-examples/helpdesk",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "vitest run",
    "clean": "rm -rf dist .tsbuildinfo"
  },
  "dependencies": {
    "@nexora/adapters": "workspace:*",
    "@nexora/architectures": "workspace:*",
    "@nexora/context": "workspace:*",
    "@nexora/contracts": "workspace:*",
    "@nexora/core": "workspace:*",
    "@nexora/gateway": "workspace:*",
    "@nexora/store-json": "workspace:*",
    "@nexora/tools": "workspace:*",
    "@nexora/transport": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "nexora": {
    "stability": "stable"
  }
}
